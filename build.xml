<?xml version="1.0" encoding="UTF-8"?>
<project name="e-gov_V2.1.0" default="default" basedir=".">
    <description>Builds, tests, and runs the project e-gov V2.1.0.</description>
    <import file="nbproject/build-impl.xml"/>

    <!-- ===================== -->
    <!-- Project properties -->
    <!-- ===================== -->
    <property name="lib.dir" value="lib"/>
    <property name="build.dir" value="build"/>

    <!-- ===================== -->
    <!-- Classpath definitions -->
    <!-- ===================== -->
    <path id="main.classpath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>

    <path id="test.classpath">
        <pathelement location="build/classes"/>
        <path refid="main.classpath"/>
    </path>

    <!-- ===================== -->
    <!-- Test target -->
    <!-- ===================== -->
    <target name="test" depends="compile">
        <mkdir dir="build/test-classes"/>

        <javac srcdir="test"
               destdir="build/test-classes"
               includeantruntime="false">
            <classpath refid="test.classpath"/>
        </javac>

        <java classname="org.junit.platform.console.ConsoleLauncher"
              fork="true"
              failonerror="true">
            <classpath>
                <path refid="test.classpath"/>
                <pathelement location="build/test-classes"/>
            </classpath>
            <arg value="execute"/>
            <arg value="--scan-class-path"/>
        </java>
    </target>

    <!-- ===================== -->
    <!-- Checkstyle -->
    <!-- ===================== -->
    <target name="checkstyle" depends="compile">
        <echo message="Running Checkstyle analysis..."/>
        <java classname="com.puppycrawl.tools.checkstyle.Main"
              fork="true"
              failonerror="true">
            <classpath refid="main.classpath"/>
            <arg value="-c"/>
            <arg value="config/checkstyle/checkstyle.xml"/>
            <arg value="src"/>
        </java>
    </target>

    <!-- ===================== -->
    <!-- PMD (Correct Ant Task Version) -->
    <!-- ===================== -->
    <target name="pmd" depends="compile">

    <echo message="Running PMD analysis..."/>

    <!-- Load ALL jars from lib (including slf4j) -->
    <path id="pmd.lib.path">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>

    <!-- Define PMD Ant task -->
    <taskdef name="pmd"
             classname="net.sourceforge.pmd.ant.PMDTask">
        <classpath refid="pmd.lib.path"/>
    </taskdef>

    <!-- Run PMD -->
    <pmd rulesetfiles="config/pmd/ruleset.xml"
         failOnRuleViolation="false">
        <formatter type="html" toFile="pmd-report.html"/>
        <fileset dir="src">
            <include name="**/*.java"/>
        </fileset>
    </pmd>

    <echo message="PMD report generated: pmd-report.html"/>

</target>


    <!-- ===================== -->
    <!-- Composite target -->
    <!-- ===================== -->
    <target name="quality" depends="checkstyle,pmd"/>

</project>
